CREATE DATABASE admin;

USE admin;

CREATE TABLE IF NOT EXISTS login (
    id INT AUTO_INCREMENT PRIMARY KEY,
    usuario VARCHAR(100),
    senha VARCHAR(100)
);

CREATE TABLE estado (
    id INT AUTO_INCREMENT PRIMARY KEY,
    UF VARCHAR(2) NOT NULL
);

CREATE TABLE marca (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome_marca VARCHAR(100) NOT NULL
);

CREATE TABLE produto (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome_produto VARCHAR(100) NOT NULL,
    valor_produto DECIMAL(10, 2) NOT NULL,
    marca_id INT,
    FOREIGN KEY (marca_id) REFERENCES marca(id)
);

CREATE TABLE cidade (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome_cidade VARCHAR(100) NOT NULL,
    estado_id INT,
    FOREIGN KEY (estado_id) REFERENCES estado(id)
);

CREATE TABLE loja (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome_loja VARCHAR(100) NOT NULL,
    cidade_id INT,
    FOREIGN KEY (cidade_id) REFERENCES cidade(id)
);

CREATE TABLE vendedor (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome_vend VARCHAR(100) NOT NULL,
    cpf VARCHAR(11) NOT NULL UNIQUE,
    cadastro DATE NOT NULL
    loja_id INT,
    FOREIGN KEY (loja_id) REFERENCES loja(id)
);


INSERT INTO login (usuario, senha) VALUES ('Baunilia', 'admin123'),('Mariana','admin123');
INSERT INTO cidade (nome_cidade) VALUES ('Bauru'),('São Paulo'),('Piratininga');
INSERT INTO estado (uf) VALUES ('SP');
INSERT INTO loja (nome_loja) VALUES ('Torra Torra'), ('Riachuelo'), ('Renner');
INSERT INTO marca (nome_marca) VALUES ('Nike'), ('Adidas'), ('Puma');